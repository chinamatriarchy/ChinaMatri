# 🔑 使用 API Token 部署（绕过 CLI 登录）

## 方法 1: 通过网页获取 Token 并部署

### 步骤 1: 创建 Token

1. **访问 Token 管理页面：**
   ```
   https://www.sanity.io/manage/personal/tokens
   ```

2. **创建新 Token：**
   - 点击 **"Add API token"** 或 **"Create new token"**
   - Name: `Deploy Token`
   - Permissions: 选择 **"Editor"** 或 **"Admin"**
   - 点击 **"Create"**

3. **复制 Token**（只显示一次！）
   ```
   sk...（一长串字符）
   ```

### 步骤 2: 使用 Token 部署

**在终端运行：**
```bash
cd /Users/xiaowanyu/03_孵化项目/ChinaMatri/studio

# 设置 token（替换成您的实际 token）
export SANITY_AUTH_TOKEN=sk开头的那串token

# 部署
npx sanity deploy
```

---

## 方法 2: 通过 Sanity 网页界面部署（最简单！）

### 可能 Sanity 提供了网页部署功能：

1. **访问项目管理：**
   ```
   https://www.sanity.io/manage/personal/project/8i1xhvuq
   ```

2. **查找部署选项：**
   - 左侧菜单查找 **"Deploys"** 或 **"Studio"**
   - 点击 **"Deploy Studio"** 按钮

3. **上传 Studio 文件：**
   - 可能需要构建 Studio：
   ```bash
   cd /Users/xiaowanyu/03_孵化项目/ChinaMatri/studio
   npm run build
   ```
   - 然后上传 `dist` 文件夹

---

## 方法 3: 本地分享 + 未来再部署

### 现在的方案：

**您的 Studio 已经在运行：**
```
本地访问: http://localhost:3333
团队访问: http://192.168.2.65:3333
```

**团队成员需要：**
1. 与您在同一网络
2. 被邀请到项目（在 Sanity 网站邀请）
3. 登录 Sanity 账户
4. 访问 `http://192.168.2.65:3333`

**邀请团队成员：**
```
https://www.sanity.io/manage/personal/project/8i1xhvuq
→ Team → Invite members
```

---

## 方法 4: 简化的 CLI 登录

### 尝试旧版登录方式：

```bash
cd /Users/xiaowanyu/03_孵化项目/ChinaMatri/studio

# 使用 managed 登录
npx sanity login --sso

# 或者使用 provider
npx sanity login --provider github
# 或
npx sanity login --provider google
```

---

## 🎯 我的推荐（现在立即可用）

### 立即方案：本地分享

**1. 确保 Studio 在运行：**
```bash
cd /Users/xiaowanyu/03_孵化项目/ChinaMatri/studio
npm run dev
# 运行在 http://localhost:3333
```

**2. 邀请团队成员到项目：**
- 访问：https://www.sanity.io/manage/personal/project/8i1xhvuq
- 点击 Team → Invite members
- 输入邮箱 → 选择 Editor → 发送邀请

**3. 分享给团队（同一网络）：**
```
CMS 管理地址：
http://192.168.2.65:3333

网站预览：
http://192.168.2.65:8000
```

**4. 团队成员操作：**
- 接受邮件邀请
- 登录 Sanity 账户
- 访问上面的 IP 地址
- 开始编辑内容！

### 优点：
- ✅ 立即可用
- ✅ 不需要解决 CLI 登录问题
- ✅ 功能完全一样
- ✅ 稍后随时可以部署到云端

### 限制：
- ⚠️ 需要同一网络
- ⚠️ 您的电脑需要开机

---

## 📞 我们现在怎么办？

### 选项 A: 使用本地分享（立即可用）✅
**现在就可以让团队开始工作**
1. Studio 已在运行：`http://192.168.2.65:3333`
2. 去 Sanity 网站邀请团队成员
3. 分享 IP 地址
4. 开始工作！

### 选项 B: 创建 Token 后部署 🔑
1. 访问：https://www.sanity.io/manage/personal/tokens
2. 创建 token
3. 使用 token 部署

### 选项 C: 稍后再部署 ⏰
1. 先让团队试用本地版本
2. 收集反馈
3. 稍后解决部署问题

---

## 💡 建议

**今天（现在）：**
- ✅ 使用本地分享
- ✅ 邀请团队成员
- ✅ 让大家开始试用

**本周内：**
- 🔧 解决 CLI 登录问题
- 🚀 部署到云端
- 🌐 获得永久 URL

---

**您想选择哪个选项？我觉得选项 A（本地分享）最快最简单！** 😊





