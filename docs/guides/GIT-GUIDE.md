# Git 版本管理指南

## ✅ 当前状态

您的项目已经成功初始化 Git 版本控制！

- ✅ Git 仓库已创建
- ✅ 所有文件已提交 (commit: f7632b6)
- ✅ 版本标签 v1.0 已创建
- ✅ .gitignore 已配置

## 📌 重要版本标记

### v1.0 - 基础版本（当前）
- 完整的 7 个页面
- 交互式地图功能
- 响应式设计
- 完整文档

## 🔄 常用 Git 命令

### 查看状态
```bash
# 查看当前状态
git status

# 查看提交历史
git log --oneline

# 查看所有标签
git tag
```

### 保存修改
```bash
# 1. 查看改动
git status

# 2. 添加修改的文件
git add .                    # 添加所有修改
git add index.html          # 添加单个文件

# 3. 提交修改
git commit -m "描述你的修改"

# 示例：
git commit -m "添加了新的女神数据"
git commit -m "优化了地图性能"
git commit -m "修复了移动端显示问题"
```

### 创建分支（重要！）
```bash
# 创建新分支进行实验性修改
git branch feature-new-design    # 创建分支
git checkout feature-new-design  # 切换到分支

# 或者一步完成
git checkout -b feature-new-design

# 查看所有分支
git branch

# 切换回主分支
git checkout main

# 合并分支
git merge feature-new-design
```

### 回退到之前的版本
```bash
# 方式 1: 回退到 v1.0 标签
git checkout v1.0

# 方式 2: 回退到某个提交
git checkout f7632b6

# 回到最新版本
git checkout main

# 完全重置到 v1.0（⚠️ 会丢失之后的修改）
git reset --hard v1.0
```

### 查看差异
```bash
# 查看未提交的修改
git diff

# 查看两个版本的差异
git diff v1.0 v1.1

# 查看某个文件的修改
git diff index.html
```

## 🌿 推荐工作流程

### 方案 1: 主分支 + 功能分支

```bash
main (稳定版本)
  │
  ├─ feature-database (添加数据库功能)
  ├─ feature-search (改进搜索)
  └─ feature-admin (后台管理)
```

**工作流程：**
```bash
# 1. 从主分支创建新功能分支
git checkout -b feature-database

# 2. 在分支上开发
# ... 修改文件 ...
git add .
git commit -m "添加数据库连接"

# 3. 功能完成后，合并回主分支
git checkout main
git merge feature-database

# 4. 如果不需要分支了，可以删除
git branch -d feature-database
```

### 方案 2: 版本标签

```bash
# 每次重大更新打标签
git tag -a v1.1 -m "添加了数据库功能"
git tag -a v1.2 -m "添加了用户系统"
git tag -a v2.0 -m "重大改版"

# 查看所有版本
git tag

# 回到任意版本
git checkout v1.1
```

## 🔐 推送到 GitHub（可选）

### 首次推送
```bash
# 1. 在 GitHub 创建仓库（不要初始化 README）

# 2. 连接远程仓库
git remote add origin https://github.com/你的用户名/ChinaMatri.git

# 3. 推送
git push -u origin main

# 4. 推送标签
git push --tags
```

### 日常推送
```bash
# 推送修改
git push

# 拉取更新
git pull
```

## 🛡️ 安全实践

### 1. 修改前先创建分支
```bash
# ❌ 不好的做法：直接在 main 分支修改
git add .
git commit -m "尝试新功能"

# ✅ 好的做法：创建分支测试
git checkout -b test-feature
git add .
git commit -m "尝试新功能"
# 如果不满意，直接切换回 main 即可
git checkout main
```

### 2. 重要节点打标签
```bash
# 每次完成重要功能后
git tag -a v1.1 -m "功能描述"
```

### 3. 定期提交
```bash
# 不要等所有功能都完成才提交
# 每完成一个小功能就提交一次

git commit -m "添加了女神数据：女娲"
git commit -m "优化了地图加载速度"
git commit -m "修复了搜索bug"
```

## 📝 提交信息规范

### 好的提交信息
```bash
git commit -m "添加：新增观音女神资料"
git commit -m "修复：地图标记点击无响应"
git commit -m "优化：提升移动端加载速度"
git commit -m "文档：更新 README 安装说明"
```

### 提交类型
- **添加**: 新功能、新内容
- **修复**: Bug 修复
- **优化**: 性能优化、代码重构
- **文档**: 文档更新
- **样式**: UI/样式调整
- **测试**: 添加测试

## 🎯 实用场景

### 场景 1: 尝试新设计
```bash
# 创建设计分支
git checkout -b new-design

# 修改样式
# ... 编辑 CSS ...

git add .
git commit -m "尝试新的紫色主题"

# 如果喜欢，合并到主分支
git checkout main
git merge new-design

# 如果不喜欢，直接删除分支
git checkout main
git branch -D new-design
```

### 场景 2: 回退错误修改
```bash
# 如果修改还没提交
git checkout -- index.html    # 恢复单个文件
git checkout -- .              # 恢复所有文件

# 如果已经提交，但还没推送
git reset --soft HEAD~1        # 撤销提交，保留修改
git reset --hard HEAD~1        # 撤销提交，丢弃修改

# 回到任意版本
git checkout v1.0
```

### 场景 3: 查看历史版本
```bash
# 查看提交历史
git log --oneline --graph --all

# 查看某个文件的修改历史
git log -- index.html

# 查看某次提交的详细信息
git show f7632b6
```

## 🔧 Git 配置（首次使用）

```bash
# 设置用户名和邮箱
git config --global user.name "你的名字"
git config --global user.email "your.email@example.com"

# 设置默认编辑器
git config --global core.editor "code --wait"  # VS Code
git config --global core.editor "vim"          # Vim

# 查看配置
git config --list
```

## 📊 可视化工具（推荐）

### VS Code 插件
- **GitLens**: 强大的 Git 增强工具
- **Git Graph**: 可视化分支图
- **Git History**: 查看文件历史

### 桌面客户端
- **GitHub Desktop**: 适合初学者
- **Sourcetree**: 功能强大
- **GitKraken**: 界面美观

### 在线平台
- **GitHub**: 最流行的代码托管平台
- **Gitee**: 国内 GitHub 替代品
- **GitLab**: 开源替代方案

## 🚨 紧急情况处理

### 误删文件
```bash
# 恢复被删除的文件
git checkout HEAD -- 文件名

# 或者
git restore 文件名
```

### 提交了错误的内容
```bash
# 修改最后一次提交
git commit --amend

# 回退到上一个提交
git reset --soft HEAD~1
```

### 想要放弃所有修改
```bash
# ⚠️ 警告：会丢失所有未提交的修改
git reset --hard HEAD
git clean -fd  # 删除未跟踪的文件
```

## 📅 建议的版本规划

```
v1.0 ✅ - 基础版本（当前）
│
├─ v1.1 - 添加 JSON 数据文件
├─ v1.2 - 优化图片和性能
├─ v1.3 - 添加更多内容
│
v2.0 - 添加后端和数据库
│
├─ v2.1 - 用户系统
├─ v2.2 - 内容管理后台
│
v3.0 - 高级功能版本
```

## 💡 快速参考

```bash
# 查看状态
git status

# 提交修改
git add .
git commit -m "说明"

# 创建分支
git checkout -b 分支名

# 切换分支
git checkout 分支名

# 回到 v1.0
git checkout v1.0

# 回到最新
git checkout main

# 查看历史
git log --oneline
```

---

## 🎉 恭喜！

您现在可以安全地修改项目了。无论做什么改动，都可以随时回到 v1.0 版本！

### 下一步建议：
1. 尝试创建一个测试分支: `git checkout -b test`
2. 在测试分支上做一些修改
3. 提交修改: `git commit -m "测试"`
4. 切换回主分支: `git checkout main`
5. 如果满意，合并: `git merge test`

如有任何问题，可以随时参考这份文档！

